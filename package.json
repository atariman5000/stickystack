{
  "name": "stickystack",
  "version": "0.1.0",
  "private": true,
  "description": "A sticky-note project board prototype with swim lanes and animated note flips.",
  "scripts": {
    "start": "python3 -m http.server 4173",
    "check": "node --check app.js && python3 -m py_compile tests/smoke.py && python3 tests/smoke.py"
  }
}
